# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'test-diffable-datasources' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'DiffableDataSources'

  # Pods for test-diffable-datasources

  target 'test-diffable-datasourcesTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'test-diffable-datasourcesUITests' do
    # Pods for testing
  end

end
